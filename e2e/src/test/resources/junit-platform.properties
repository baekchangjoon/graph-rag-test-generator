# 생성된 테스트를 병렬 실행한다 (클래스·메서드 동시, 병렬도 8).
# 도구가 합성하는 테스트는 병렬 안전을 전제로 설계됐다:
#  - read-path 시드는 엔드포인트별 비충돌 id를 쓰고, 한 엔드포인트당 첫 2xx path만 시드
#  - write-path 픽스처는 scope.testId() 기반 unique 값으로 격리
#  - 격리 불가한 테스트(외부 HTTP baggage 미전파 등)는 generator가
#    @Execution(SAME_THREAD)로 표시하므로 이 설정 하에서 직렬로 실행된다
junit.jupiter.execution.parallel.enabled=true
junit.jupiter.execution.parallel.mode.default=concurrent
junit.jupiter.execution.parallel.mode.classes.default=concurrent
junit.jupiter.execution.parallel.config.strategy=fixed
junit.jupiter.execution.parallel.config.fixed.parallelism=8
