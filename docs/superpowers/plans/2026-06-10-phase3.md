# Phase 3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. 단계별 TDD + commit + progress 기록.

**Goal:** STOMP endpoint 캡처 + STOMP 테스트 합성이 compose에서 통과.
**Spec:** `docs/superpowers/specs/2026-06-10-phase3-design.md`

### Task A: shared-model — WsEndpoint/WsExchange/GraphAsset 확장 + 라운드트립
### Task B: SUT — STOMP 설정 + orders/count 핸들러 + Spring STOMP 클라이언트 테스트
### Task C: builder — WsEndpointIndexer (config 리터럴 + @MessageMapping)
### Task D: builder — StompProbe(최소 STOMP/JDK WebSocket) + WsCaptureRunner + CLI + e2e 검증
### Task E: testlib — StompFrames 코덱 + StompHelper + TestScope.stomp()
### Task F: generator — ws 합성 (pseudo-path 변환 + ws 템플릿 + 상관관계 규칙 + 테스트)
### Task G: E2E (request-ws.json, N+α 전부 PASS) + progress/decisions/README + push + 보고
