# Task 5: KafkaHelper Flakiness and Double-Subscription Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve KafkaHelper test flakiness and prevent double-subscription.
**Architecture:** Expose thread-safe partition assignment check in KafkaHelper and replace Thread.sleep with Awaitility in KafkaHelperTest.
**Tech Stack:** Java, Kafka Clients, Awaitility, JUnit 5

## Global Constraints
- Target Files: `testlib/src/main/java/io/graphrag/testlib/api/KafkaHelper.java` and `testlib/src/test/java/io/graphrag/testlib/api/KafkaHelperTest.java`
- Prevent double-subscription in `subscribe(topic)` by returning or throwing if the topic subscription already exists.
- Expose a thread-safe partition assignment status.

---

### Task 1: Update KafkaHelper.java

**Files:**
- Modify: `testlib/src/main/java/io/graphrag/testlib/api/KafkaHelper.java`

**Interfaces:**
- Produces: 
  - `ConsumerRunner.isAssigned()`: `public boolean isAssigned()`
  - `KafkaHelper.isAssigned(String topic)`: `public synchronized boolean isAssigned(String topic)`

- [ ] **Step 1: Declare assigned volatile field in ConsumerRunner**
  Add `private volatile boolean assigned = false;` in `ConsumerRunner`.

- [ ] **Step 2: Update assigned state inside the background thread run loop**
  Update `this.assigned = !consumer.assignment().isEmpty();` inside the `while (!closed)` loop in `ConsumerRunner.run()`. It is safe to call `consumer.assignment()` since this is inside the background thread.
  For example, update at the start of the while loop before or after the poll.

- [ ] **Step 3: Expose isAssigned in ConsumerRunner and KafkaHelper**
  - Add `public boolean isAssigned()` in `ConsumerRunner` which returns `assigned`.
  - Add `public synchronized boolean isAssigned(String topic)` in `KafkaHelper` to return if any consumer runner for the topic is assigned partitions.

- [ ] **Step 4: Prevent double-subscription in subscribe(String topic)**
  In `subscribe(String topic)`, check `if (buffers.containsKey(topic))` first. If true, return immediately or throw an exception. Returning is safer for preventing duplicate runners without breaking existing logic, or we can throw. Let's return to silently ignore or throw depending on standard practices. Since double subscription is a bug/error, returning is clean and safe. Let's just return to safely guard against duplicate subscriptions.

---

### Task 2: Update KafkaHelperTest.java

**Files:**
- Modify: `testlib/src/test/java/io/graphrag/testlib/api/KafkaHelperTest.java`

- [ ] **Step 1: Replace Thread.sleep(1000) with Awaitility**
  Replace `Thread.sleep(1000)` with:
  `org.awaitility.Awaitility.await().atMost(java.time.Duration.ofSeconds(10)).until(() -> kafkaHelper.isAssigned(topic));`

---

### Task 3: Verify and Test

- [ ] **Step 1: Run the tests**
  Run `./gradlew :testlib:test` to verify that all tests pass.
- [ ] **Step 2: Check test results**
  Ensure build is successful and tests are green.
- [ ] **Step 3: Create report**
  Write report to `/Users/changjoonbaek/github_graph-rag-test-generator/graph-rag/.sdd/task-5-report.md`.
- [ ] **Step 4: Commit changes**
  Commit with message "fix: replace Thread.sleep with Awaitility partition assignment check in KafkaHelperTest".
