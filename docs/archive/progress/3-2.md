# 3.2 — STOMP 통신 캡처

- 자체 최소 STOMP 클라이언트(JDK WebSocket, `StompFrames` 코덱)로 분석 환경에서
  구독→전송→수신. happy + missing-ref 2-변형 결정적 캡처 → `WsExchange`
- 교환 구간의 SQL 로그 캡처 연동 (파생 쿼리 count SELECT + binding origin)
- **조인 별칭 귀속 결함 발견·교정**: `SqlBinding.table` 도입
  (`docs/decisions/stomp-capture.md`)
- 검수: 실 SUT 통합 — wsEndpoints/wsExchanges(2건)/echo 응답/SQL API_PARAM 검증
