# 0.5 — socket-mock-server

## 진행 내용

- Netty TCP 리스너 (expectation 등록 포트마다 1개) + byte 패턴 매칭(EXACT/PREFIX)
- admin REST: `POST /__admin/expectations` (등록 + 리스너 기동), `DELETE` (전체 해제)
- HexCodec: 공백 구분/연속 hex 모두 허용
- Dockerfile (admin 9099 / data 9000 expose)

## 검수

- `./gradlew :socket-mock-server:test` GREEN
  (HexCodec 4건 + Registry 5건 + TCP 통합 1건: 등록→raw socket 송신→응답 byte 검증)

## 설계 부합 / 메모

- docs/07 최소 기능 중 stateful 다단계 세션, regex-over-hex 매칭, UDP, 수신 hex
  레코더는 Phase 4(Netty socket 본격 지원)에서 구현. 0.5는 roadmap 정의대로 골격.
