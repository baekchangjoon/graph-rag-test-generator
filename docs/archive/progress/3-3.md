# 3.3 — testlib STOMP helper + 생성 테스트

- testlib: `StompFrames` 코덱 + `StompHelper`(connect/subscribe/send/
  awaitMessageContaining) + `TestScope.stomp(wsPath)` (cleanup 시 자동 종료)
- generator: `ws-test-class.mustache` 템플릿, WsExchange → pseudo-path 변환으로
  FixtureComposer 재사용, **echo 마커 기반 병렬 격리** (echo 없으면
  `@Execution(SAME_THREAD)` + `serial_required(WS_NO_CORRELATION)`)
- 검수: 코덱 3건 + generator WS 합성 테스트 GREEN (마커/픽스처/병렬 보고)
