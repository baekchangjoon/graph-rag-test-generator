# Phase 2 E2E (2026-06-10)

## 결과

- 도구 1: 14 path (Phase 1의 12 + EXPRESS 201 + 재고부족 409), 외부 HTTP 캡처 2건
  (둘 다 baggagePropagated=true, consumedFields=[available])
  - EXPRESS 201: heuristic의 literal 변이로 발견
  - 409: fuzzer의 2단 조합(literal-type-EXPRESS 시드 + large-amount)으로 발견
- 도구 2: 14 테스트 클래스. EXPRESS/409 테스트는 WireMock 스텁(baggage 매칭)
  등록 코드 포함. 전부 propagation 확인됨 → fully_parallel
- compose 실행(OTEL agent 부착): **tests=14 skipped=0 failures=0 errors=0**

## Phase 2 메트릭 (roadmap 09)

외부 HTTP 호출 있는 endpoint의 테스트가 WireMock 사용해 통과 + 병렬 안전 — **달성**

## 과정에서 잡은 결함

- "4xx면 픽스처 생략" 규칙이 409에서 깨짐 (409는 user 존재가 전제 — 404 검사가
  재고 검사보다 앞). **"조회 성공 증거"**(SELECT 이후 후속 SQL 또는 외부 HTTP
  호출) 기반 픽스처 결정으로 교정 — 첫 E2E에서 13/14로 실패해 발견, 수정 후 14/14

## 회귀

- `./gradlew check` 전 모듈 GREEN
