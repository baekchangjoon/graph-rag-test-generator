# Phase 1 E2E (2026-06-10)

## 실행

`./e2e/run-e2e.sh` (run-phase0.sh 대체 — path id 체계 변경으로 통합)

## 결과

- 도구 1: endpoint 2개 탐색 (budget 60 req/endpoint)
  - `post-api-orders`(JPA): 8 path — s201×1, s404×1, s400×6(검증 조건 operand별)
    handler 분기 6/12 커버
  - `post-api-orders-search`(MyBatis): 4 path — s200×3, s400×1.
    s400(필터 전부 제거)과 s200-3은 **fuzzer**가 다단 변이로 발견. 분기 3/6 커버
  - exploration-report.json에 미도달 분기(still_missing) 좌표 수록
- 도구 2: 12 path → 12 테스트 클래스 (결정적)
- compose 환경 실행: **tests=12 skipped=0 failures=0 errors=0**

## Phase 1 메트릭 (roadmap 09)

같은 endpoint의 N개 path가 N개 테스트로 합성되고 통과 — **달성**
(orders 8/8, search 4/4)

## 회귀

- `./gradlew check` 전 모듈 GREEN (빌더 실 SUT 통합 포함)

## 미도달 분기에 대해 (정직하게)

미커버 분기(orders 6, search 3)는 JaCoCo 분기 카운터의 단조 키 표현상
"반대 방향 operand" 등이며, exploration-report.json에 라인 좌표로 남는다.
docs/22의 분류·수동 등록(--manual-paths) 경로로 보강 가능.
