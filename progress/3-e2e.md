# Phase 3 E2E (2026-06-10)

## 결과

- 도구 1: HTTP 14 path + WS 교환 2건 (happy/missing-ref, echo 응답 + 파생 쿼리 SQL)
- 도구 2: 16 테스트 클래스 (HTTP 14 + STOMP 2, 둘 다 echo 마커로 fully_parallel)
- compose 실행: **tests=16 skipped=0 failures=0 errors=0**

## 과정에서 잡은 결함

- 첫 실행 14/16: WS 파생 쿼리의 **조인 별칭이 바인딩 테이블을 오귀속**
  (users.id 바인딩 → orders로) → BIGSERIAL PK에 문자열 INSERT 픽스처.
  `SqlBinding.table`(별칭 해석) 도입으로 교정 — E2E가 또 한 번 합성 규칙의
  결함을 잡아낸 사례

## 회귀

- `./gradlew check` 전 모듈 GREEN
