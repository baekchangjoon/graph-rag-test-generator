# 0.8 — Phase 0 E2E

## 진행 내용

- `e2e/docker-compose.yml`: postgres:15 + SUT(order-service) + wiremock +
  socket-mock-server + test-state-dashboard (docs/06 구성의 Phase 0 부분집합)
- `e2e/run-phase0.sh`: jar 빌드 → 도구 1(graph 빌드, Testcontainers 분석 환경) →
  도구 2(테스트 생성) → compose 기동 → 생성 테스트 실행 → 정리
- `e2e` 모듈: 생성 테스트를 `build/generated-tests`에서 컴파일/실행.
  `APP_BASE_URI` 미설정 시 test task 스킵 (일반 `gradlew check` 보호)

## 검수 (2026-06-10 실행 기록)

- `./e2e/run-phase0.sh` → **✅ Phase 0 E2E PASS**
- 테스트 리포트: `OrdersPostTest` tests=1, skipped=0, failures=0, errors=0 (1.016s)
- 산출물: `e2e/out/graph/graph.json`(도구 1), `e2e/out/generated/.../OrdersPostTest.java`
  + `generation-result.json`(도구 2)
- Phase 0 메트릭 (roadmap 09): **1/1 endpoint 생성 테스트 통과**

## 트러블슈팅

- 호스트 포트 55432가 이전 프로젝트 컨테이너(graphrag-scout-pg)에 점유 →
  56432로 변경 (외부 컨테이너는 건드리지 않음)

## 추가 검증 (Phase 1 시작 시 수행, 2026-06-10)

- 전 사이클 2회 연속 실행: 둘 다 PASS. 생성 테스트 `.java`와 `graph.json` 모두
  사이클 간 byte 동일 → cleanup·결정성 재검증 완료
- GitHub Actions CI 추가 (`.github/workflows/ci.yml`): check + Phase 0 E2E.
  로컬 전용 `org.gradle.java.home`은 CI에서 `GRADLE_OPTS`로 덮어씀
