# Phase 7 E2E (2026-06-14)

## 결과

- 도구 1: auth POST 10 path + search 4 path + WS 2 교환 + GET-by-id 3 path + GET-by-userId 3 path
- 도구 2: 22 테스트 클래스
- compose 실행: **tests=22 skipped=0 failures=0 errors=0**
  (auth POST 10, search 4, WS 2, GET-by-id 3, GET-by-userId 3)

## 과정에서 잡은 결함 (통합 수정 commit 506bf7a)

- `mapParamToColumn` 이 `xxxId` 파라미터를 PK로 강제 매핑 — FK 컬럼 구분 로직으로 교정
- path-param 센티넬 치환 누락 → URI 조합 crash — 드롭 파라미터 대체 sentinel 삽입으로 교정
- Postgres IDENTITY 시퀀스가 명시 id=1 INSERT 이후 1부터 재시작 → `setval` 재동기로 교정
- `FixtureComposer` 숫자 리터럴 타입 불일치 + child-first seed 삭제 순서 오류 → type-aware + 역정렬로 교정

## 회귀

- `:graph-rag-builder:test` 전 단위 GREEN
- `./gradlew check` 전 모듈 GREEN
