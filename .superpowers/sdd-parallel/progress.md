# Parallel fan-out builder — decision B (full pjacoco replace, single path)
base: f10689d
Design rev.2 committed 5d8cef5 (3-vendor reviewed). Inbox ROI decision TIMEOUT → safe_default: Phase 0부터 단계적 전체 진행. Next: requirements → plan → Phase 0 impl.
Phase 0 (REQ-P001): locked P=8 1.91x (commit add7f61). Phase 0.5 UNLOCKED P=8 2.01x (commit 07c99ee) — lock removal +0.10x=NOISE. BOTTLENECK = single-SUT HTTP saturation + CPU contention, NOT dump lock. Real speedup ceiling ≈2x (NOT 3.72x). Phase 1 pjacoco won't raise it (bottleneck isn't coverage mechanism).
DECISION POINT: ~2x marginal ROI for multi-week irreversible pjacoco migration. Higher speedup would need SUT replication (PoC-discarded option B). Routed to inbox; safe_default=PAUSE+report (don't auto-start Phase 1 on marginal ROI).
DECISION (user, opt 1): 단일 SUT pjacoco 그대로 진행 (~2x 수용). Resume staged impl: Phase 1 (pjacoco swap + --parallelism 1 set-equiv gate) → Phase 2 (parallelize). Authoring Phase 1 plan.
Phase 1 Task P1-1: DONE (commit aca871e) — PjacocoAgent+PjacocoCoverageBackend in main, pjacoco via mavenLocal→bundled resource, unit 5/5 green. CI needs pjacoco :agent:publishToMavenLocal step. NEXT: P1-2 agent attach swap (AnalysisEnvironment+runAttached).
Phase 1 P1-2: DONE (fca5195) — agent attach JaCoCo→pjacoco both paths, --coverage-backend flag, jacoco preserved, compile+tests green, smoke deferred. NEXT P1-3 coverage collection swap.
Phase 1 P1-3: DONE (baaf0fb) — CoverageProbe abstraction (Jacoco/Pjacoco), per-request traceparent, doSend+Kafka/Ws swap, compile+unit green, single-ep pjacoco deferred to P1-5. NEXT P1-4 set-diff tool + oracle determinism.
Phase 1 P1-4: DONE (1370ff6, verified on branch) — GraphSetEquivDiffTool (10 fields, branch-coord keys avoid coverageKey hash diff) + graph-diff.sh, 9 unit green. oracle determinism deferred to P1-5. NEXT P1-5 hard gate (live SUT set-equiv).
Phase 1 P1-5: HARD GATE PASS (c173425) — order-service --parallelism 1 pjacoco == JaCoCo SET-EQUIVALENT (28ep/157paths/72sql all match), regression 208 tests 0 fail. pjacoco preserves exploration power (decision B core risk resolved).
SEQUENCING ADJ: P1-6 (full JaCoCo removal) deferred to AFTER Phase 2 validation — keep --coverage-backend jacoco as oracle through Phase 2, remove last. Phase 1 functionally complete.
NEXT: consolidated Phase 1 code review (aca871e..c173425) then Phase 2.
Phase 1 review (aca871e..c173425): Critical 0, Important 2 (Phase-2 blockers) + 3 Minor. FIXED (commit c604783): I1 shared TraceParent (SHA-256(runId+AtomicLong), single instance injected to all runners L716/L841) → no traceId collision in Phase 2; I2 WS noCoverageDelta fast-path → no 30s/exchange timeout; I3 includes fallback. compile+unit green.
WATCH-ITEM(Phase 2 gate): OtelSpanCapture uses a separate TraceParent(same runId) for SQL capture — parallel set-equiv gate REQ-P009 will catch any SQL traceId mis-correlation.
=== PHASE 1 COMPLETE (functional): pjacoco swap set-equivalent to JaCoCo (order-service), reviewed+fixed. NEXT: Phase 2 parallelization (per-worker accumulator/Connection, seed-key, async flush, parallel set-equiv). JaCoCo full removal (P1-6) deferred to after Phase 2.
Phase 2 P2-1/2: DONE (committed) — post-loop single-threaded merge of 11 categories (cumulativeExec OR-merge L896 merge-only), per-worker Connection (workerConn try-with-resources), no concurrent shared writes. compile+unit green. NEXT P2-3 seed key collision.
Phase 2 P2-3: DONE (faf5ab0) — endpoint-scoped probe keys (probe-{tag}-{field}), 7 files, 160 tests green, seed collision fixed.
Phase 2 P2-4: DONE (da6afbf) — backend concurrent-safe (final/immutable fields), per-worker-sync flush, --exec-await-ms/--flush-threads flags, 8-thread test green.
Phase 2 P2-5: GATE RUN COMPLETE (harness committed) — order-service --parallelism 4 (1311s, 3.03x speedup). FAIL: sql extra 156 (log-parser fallback SQL cross-contamination in parallel), httpCalls missing 2 (HttpCaptureServer shared drain race). race/seed-collision=0 (P2-1~4 correct). NEXT: fix F1(log-parser fallback parallel disable) + F2(httpCapture per-traceId) → re-gate P2-5.
Phase 2 P2-5 (c3cce29): FAIL but valuable — 9/11 fields set-equiv, race/seed 0, SPEEDUP 3.03x (66m→22m P=4, >2x target). 2 builder bugs: F1 sql +156 (OtelSpanCapture log-parser timestamp fallback cross-attributes under parallel log overlap); F2 httpCalls -2 (HttpCaptureServer.drainNewExchanges shared drain race). NEXT: fix F1/F2 + re-gate. P1-6 blocked until P2-5 green.
Phase 2 P2-5 재게이트 (189c1d2, 2026-06-24): FAIL. T_par4=1211s, speedup=3.28x, race=0. F1 오염 제거 ✅ 그러나 OTel timeout → sql MISSING 65 (3/68). F2 drainByTraceId 구현 ✅ 그러나 SUT baggage 미전파 → httpCalls MISSING 2. seeds MISSING 1 (신규). F1b(OTel timeout 개선 또는 per-traceId timestamp-window log-parser)/F2b(timestamp-window per-worker drain) 추가 수정 필요.
Phase 2 P2-5 re-gate (373808f): STILL FAIL but shifted. 8/11 equiv, race-clean, speedup 3.28x. sql: F1 fixed contamination but OTLP timeout(8s) drops SQL under 4-worker load (timeout-tunable). httpCalls(2): drainByTraceId correct but order-service doesn't propagate baggage outbound (SUT-dependent, hard). seeds 1 timing. DECISION→inbox.
Phase 2 P2-5 F1b+httpCalls 정정 (2026-06-24, 재부팅 후 세션): (a) F1b SQL await 8s→30s 병렬 기본값 + --sql-await-ms (OtelSpanCapture/BuildConfig/BuilderCli, 미커밋분 검증·컴파일 green). (b) **httpCalls 한계 결론 철회**: handoff "조기 한계수용 금지" 따라 OTel agent 부착 직접 검증 → order-service는 javaagent(OTEL_PROPAGATORS=tracecontext,baggage)+RestTemplate로 inbound baggage(test.id)를 outbound로 **정상 전파**함을 통합테스트 OtelHttpCaptureIntegrationTest.inboundBaggage_propagatesToOutboundInventoryCall로 입증(exchanges=1 /inventory/stock baggagePresent=true, drainByTraceId 귀속 OK). 직전 "미전파" 근거는 HttpCaptureServer baggagePresent 플래그가 test-id=(하이픈) vs 실제 test.id=(점) 검사하던 **false-negative 버그**였음 → 수정. (c) 병렬 httpCalls=0의 진짜 원인은 baggage 아님 → 재게이트로 규명(부하 하 요청/통합 경로). 문서(design §9-A·requirements·이 progress) 증거기반 정정. NEXT: F1b 빌더로 P2-5 재게이트(전 11필드 set-동등 목표).
Phase 2 P2-5 근본원인 규명 (2026-06-24, systematic-debugging): par4 sql ~96% timeout + httpCalls 0의 진짜 원인 = pjacoco includes 과대범위. PjacocoAgent.detectRootPackage가 sutSrc 첫 세그먼트만 취해 io.*(io.opentelemetry.*=OTel span-export/baggage propagator + io.netty.* 포함)를 계측 → 병렬에서 OTel export starve(sql 누락)+baggage 전파 간섭(httpCalls 누락). 순차는 오버헤드 감내. 규명: ①collision 기각(307 timeout 전부 distinct) ②otel-only 4-way 즉시-drain 12/12 통과(동시성 무해) ③JAVA_TOOL_OPTIONS includes=io.* 발견 ④--sut-pkg io.graphrag.* 병렬빌드 timeout 0/sql 47(확정). 수정: detectRootPackage 단일자식 체인 하강→io.graphrag.sample.orders.* (PjacocoAgentTest). 코드수정 e2e검증(자동탐지 budget16 P=4): timeout 0, sql 59, httpCalls 2, seeds 19 전부 회복. F1b(30s)=증상처방→2차방어로 유지. NEXT: 풀 재게이트 11/11.
Phase 2 P2-5 풀 재게이트 PASS (2026-06-24): seq vs par4 SET-EQUIVALENT — 11/11 전 필드(endpoints28/paths157/sql72/httpCalls2/seeds22/capturedEventEmits2/ws/kafka 전부 일치). speedup 2.54x, race=0. 근본수정 2건: ①pjacoco includes 앱루트화(io.*→io.graphrag.sample.orders.*) ②KafkaCaptureReceiver.drainByTraceIds 병렬안전(buildPaths/kafka-diff 자기 traceId만 수거, drainAllByTraceId 전역clear race 해소; KafkaCaptureReceiverParallelDrainTest). 부수효과: T_seq 3978s→145s(27배). REQ-P004/P009 🟢. NEXT: P1-6(JaCoCo 전면제거) — 머지단계 인박스 위임(secretary Q3).
